{
  "name": "game-one-defense",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "BASTION — un jeu de défense tactique complet, en français.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/engine.js && node --check src/render.js && node --check src/data.js && node --check src/audio.js && node --check src/camera.js",
    "balance": "node scripts/balance.mjs"
  },
  "engines": { "node": ">=20" }
}
